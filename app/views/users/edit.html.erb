<% content_for :title, "Editing user" %>

<h1 class="font-bold text-4xl mbe-4">Editing user</h1>

<%= render "form", user: @user %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Show this user", @user, class: "btn" %>
  <%= link_to "Back to users", users_path, class: "btn" %>
</div>
